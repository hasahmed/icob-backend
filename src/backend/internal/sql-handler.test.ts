import 'jest';
import { SqlHandler } from './sql-handler';

describe('SqlHandler Tests', () => {
	it('should exist', () => {
		expect(SqlHandler).toBeTruthy();
	});
	it('Should create an account', () => {
	});
});
